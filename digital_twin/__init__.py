"""Offline growth simulation for BioReact-Pi.

See ``digital_twin/simulator.py`` for the simulation loop; import from there
directly (``from digital_twin.simulator import simulate``) rather than from
this package root, so ``python -m digital_twin.simulator`` doesn't trigger a
duplicate-import warning.
"""
