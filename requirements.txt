# BioReact-Pi — UI dashboard & API

# Cloud API & dashboard
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
Pillow>=10.0.0

# Testing
pytest>=8.0.0

# Edge / Pi (uncomment when hardware is wired)
# RPi.GPIO
# Adafruit-DHT
# python-dotenv

# AI advisor — optional; the "Ask AI" button degrades to a clear
# "not configured" message without it (see ui/api/advisor.py).
# NOTE: google-generativeai is deprecated/unmaintained — this is the
# current Gemini SDK.
google-genai>=0.3.0

# Voice narration of the advisor's answer — optional; the speaker button
# degrades to a clear error if unset (see ui/api/voice.py).
elevenlabs>=1.5.0

# Telemetry history — optional; /api/history returns an empty list and the
# WS loop just skips persistence if unset (see ui/api/db.py).
pymongo>=4.6.0
