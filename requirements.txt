# BioReact-Pi — UI dashboard & API

# Cloud API & dashboard
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
Pillow>=10.0.0

# Testing
pytest>=8.0.0

# Edge / Pi deps (flask, opencv, tflite-runtime) live in edge/requirements.txt
# instead — that installs on the Pi itself, not here on the dashboard machine.
# python-dotenv

# AI & voice (uncomment when cloud services are wired)
# google-generativeai
# elevenlabs
